#include <stdio.h>
int sum_of_cubes(int n,int m){
int total_sum=0;
for(int i=n;i<=m;i++)
{
total_sum+=i*i*i;
}
return total_sum;
}
int main(){
int n=2,m=4;
int result=sum_of_cubes(n,m);


    printf("sum_of_cubes from %d to %d\n",n,m,result);

    return 0;
}
