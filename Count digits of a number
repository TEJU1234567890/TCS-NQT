//Count digits of a number.Input: n = 2446
Output: 1
Explanation: Here among 2, 4, 6 only 2 divides 2446 evenly while 4 and 6 do not.//


def counts_dividing_digit(n):
  count=0
  for digit in str(n):
      d=int(digit)
      d!=0 and n%2==0
      count+= 1
      return count
      
print(counts_dividing_digit(2446))
